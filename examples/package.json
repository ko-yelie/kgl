{
  "name": "@ko-yelie/kgl-example",
  "private": true,
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html --no-source-maps --out-dir ../docs --public-url /kgl/",
    "pages": "rm -rf ../docs && npm run build"
  },
  "devDependencies": {
    "@babel/core": "^7.17.10",
    "@babel/plugin-transform-runtime": "^7.17.10",
    "parcel-bundler": "1.12.5",
    "stylus": "^0.57.0"
  },
  "dependencies": {
    "glsl-curl-noise": "0.0.4",
    "glsl-random": "0.0.5",
    "glsl-y-rotate": "^3.0.0"
  }
}
