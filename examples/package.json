{
  "name": "@ko-yelie/kgl-example",
  "private": true,
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html --no-source-maps -d ../docs --public-url /kgl/",
    "pages": "rm -rf ../docs && npm run build"
  },
  "devDependencies": {
    "@babel/core": "^7.15.0",
    "@babel/plugin-transform-runtime": "^7.15.0",
    "parcel-bundler": "^1.12.5",
    "stylus": "^0.55.0"
  }
}
